<Window x:Class="Lékárna.MedicineViewer"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lékárna"
        mc:Ignorable="d"
        Title="MainWindow" Height="1080" Width="1920" MinHeight="350" MinWidth="500" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid>
        <ListView Margin="0,20,850,0" Name="Medicine">
            <ListView.View>
                <GridView >
                    <GridViewColumn Header="Name" Width="250" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Allergen" Width="300" DisplayMemberBinding="{Binding Allergen}" />
                    <GridViewColumn Header="Price" Width="220" DisplayMemberBinding="{Binding Price}" />
                    <GridViewColumn Header="Important" Width="300" DisplayMemberBinding="{Binding Important}" />
                </GridView>
            </ListView.View>
        </ListView>
        <Label x:Name="UserName" Content="Name" HorizontalAlignment="Right" FontSize="80" Width="240" Margin="0,10,286,936"/>

        <Label x:Name="ShoppingList" Content="Shopping list" Margin="1062,115,528,-114" FontSize="35"></Label>
        <ListView Margin="1062,162,0,603" Name="ShopList" HorizontalAlignment="Left" Width="821">

            <ListView.View>
                <GridView >
                    <GridViewColumn Header="Name" Width="250" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Price" Width="220" DisplayMemberBinding="{Binding Price}" />
                    <GridViewColumn Header="Amount" Width="388" DisplayMemberBinding="{Binding Amount}" />
                </GridView>
            </ListView.View>
        </ListView>
        <Label x:Name="Price" Content="Price" Margin="1415,724,392,257" FontSize="40" FontWeight="Bold"></Label>
        <Button x:Name="Buy" Content="Buy" HorizontalAlignment="Right" Margin="0,797,362,0" VerticalAlignment="Top" Width="164" Height="58" FontSize="40" FontWeight="Bold"/>
        <Button x:Name="Settings" Content="Add new allergen" HorizontalAlignment="Left" Margin="1146,961,0,0" VerticalAlignment="Top" Width="316" FontSize="30" Height="42"/>
        <Button x:Name="Logout" Content="Logout" HorizontalAlignment="Left" Margin="1492,961,0,0" VerticalAlignment="Top" Width="314" FontSize="30" Height="42"/>


    </Grid>
</Window>
